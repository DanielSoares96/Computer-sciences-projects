fact (n: int) : int {
  f, i : int = 1;
  
  while i <= n then {
    f=f*i;
    i=i+1;
  };
  
  return f;
};

main () : void {
  x : int;
  i : int;
  print("Introduza_um_inteiro");
  input(x);
  print("O_factorial_de");
  print(x);
  print("e");
  i = fact(x);
  print(i);
};
